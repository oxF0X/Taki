<Window x:Class="TakiClient.Views.JoinedRoomView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TakiClient.Views"
        mc:Ignorable="d"
        Title="JoinedRoomView" Height="450" Width="800"
        xmlns:viewModel="clr-namespace:TakiClient.ViewsModels">

    <Window.DataContext>
        <viewModel:JoinedRoomViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition/>


        </Grid.RowDefinitions>

        <Button Grid.Row="1" Style="{StaticResource LoginButtonStyle}" Content="Leave room :(" Command="{Binding LeaveRoomCommand}"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Users}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5">
                            <TextBlock Text="{Binding}" FontSize="16" FontWeight="Bold" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        
    </Grid>

</Window>
